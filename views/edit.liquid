<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Edit - {{title}}</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="css/style.css">
    </head>
    <body>
        <h1>Update blog</h1>

         {% for txt in art %}
            <h3> --blog title-- {{ txt.blog_id }}</h3>
            <p> {{ txt.blog_txt }} </p>
            <a href="edit/{{ txt.blog_id }}">edit</a>
            <button id={{ txt.blog_id }} class="edit-button">Edit</button>
          {% endfor %}

        <hr>
        <h2>Edit stuff {{ art.blog_id }}</h2>
        <form action="/update" method="POST">
            <div>
                <label for="blog">What is your blog txt?</label>
                <input name="blog" id="blog" value={{ art.blog_txt }} />
            </div>
            <div>
                <button>Send my blog</button>
            </div>
        </form>
        <hr>
        <h2>Delete stuff</h2>
                <form action="/delete" method="POST">
            <div>
                <label for="blog">Blog to delete?</label>
                <input name="blog" id="blog" value="{{ txt.blog_id }}" />
            </div>
            <div>
                <button>delete my blog</button>
            </div>
        </form> 
    </body>
</html>